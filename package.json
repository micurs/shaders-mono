{
  "name": "shaders-mono",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "all:build": "pnpm run -r build",
    "sh:watch": "pnpm --filter geopro watch:code",
    "sh:build": "pnpm --filter geopro build && pnpm --filter webgpu build",
    "demo:build": "pnpm --filter \"demo-*\" build",
    "demo:dev": "pnpm --filter \"demo-01\" dev",
    "sh:test": "pnpm --filter geopro test",
    "start:cli": "node demos/cli/dist/geopro.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^5.0.5",
    "vitest": "^0.34.6"
  }
}
