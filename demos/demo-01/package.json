{
  "name": "demo-01",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rimraf dist && tsc && vite build",
    "pages:deploy": "cp -r dist/* ../../pages",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "@shaders-mono/geopro": "workspace:*",
    "@shaders-mono/webgpu": "workspace:*"
  }
}
